语法: alias <短版本> <长版本>
        alias <短版本> <长版本> $*

此命令允许你用一个短命令替代一个长命令。
例如，与其输入 "get all from corpse"，你可以
创建这个别名：

%^GREEN%^alias gc get all from corpse%^RESET%^

然后你只需输入 "gc" 就能达到同样的效果。
你还可以使用通配符，像这样：

%^GREEN%^alias gc get all from $* corpse%^RESET%^

这样输入: %^GREEN%^gc first%^RESET%^
会扩展为: %^GREEN%^get all from first corpse%^RESET%^

通配符对频道别名也很有用：

%^GREEN%^alias ige intergossipemote $*%^RESET%^

要查看你的别名，输入: %^GREEN%^alias%^RESET%^

要查看特定别名，如 gc: %^GREEN%^alias gc%^RESET%^

要删除别名，输入 unalias 加上别名，像这样：

%^GREEN%^unalias gc%^RESET%^
