不同的客户端有不同的行为。Dead Souls 尝试自动
处理最常见的设置。这对你来说可能不够。

以下是一些常见问题：

1) 你将屏幕设置为某个值（如 100 50），但它一直
   变成其他值。

这通常是因为你的客户端向游戏发送了不可见的调整大小
命令，而游戏接受了这些命令。要阻止这种情况，你需要
锁定屏幕设置，然后设置屏幕参数：
%^GREEN%^
screenlock on
screen 100 50
%^RESET%^

2) 你将宽度和/或高度设置为非常大的数字，但检查时
   发现它变小了。

你的屏幕设置会尝试限制在游戏最大可打印字符串大小
之内...这个数字相当随意，主要是为了避免过度刷屏
玩家。这个限制只能由愿意重新编译驱动程序并更改
LARGEST_PRINTABLE_STRING 选项值的管理员来更改。


3) 你将宽度设置为非常大的值，但并非所有文本都那么宽。

一些文本（如此文件）在第 80 列之前的某个位置写入了
硬回车。这通常是由于作者的偏好，在特定年代的代码
编写者中很常见。如果你发现特别不美观的情况，
可以和管理员商量是否要将其添加到清理列表中。

另见: env, terminal
