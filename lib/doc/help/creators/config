本文件详细说明了 /secure/include/config.h 中配置参数的效果

#ifndef s_config_h
#define s_config_h
    这意味着"如果 s_config_h 参数未定义，则现在定义它，
同时也定义此文件中的内容。"


#define DEFAULT_PROMPT           "> "
    不言自明。可以通过 "prompt" 命令为每个玩家单独覆盖。


#define HOST_IP                  "127.0.0.1"
    有时你的游戏的实际 IP 可能是一个假的内部地址，
如 10.0.1.1。但在某些情况下这可能会令人困惑，
特别是在 hftp 服务器的情况下，这样的地址会干扰
正常运行。
    通常正确且安全的做法是不要修改此参数。
定义 127.0.0.1 让游戏知道它需要以正常方式
确定其 IP 地址。如果你正在运行 hftp，或者有
某些不寻常的情况，你的实际 IP 地址不可达但
其他某个地址可达，那么你可以在这里放那个
外部可访问的 IP 地址。
    一般建议：不要修改此项。


#define MUD_STATUS               "mudlib development"
    不言自明。


#define ADMIN_EMAIL              "cratylus@comcast.net"
    不言自明。注意这里定义一个有效的电子邮件非常重要。
否则如果 I3 路由器移动，或者你的游戏出现故障需要
被列入黑名单，就无法联系你来解决问题。


#define DEBUGGER                 "cratylus"
    tc() sefun 是一种供管理员使用的特殊调试器。
它会将其消息发送给这里指定的那个人。


#define LOCAL_NEWS_PREFIX        "nm"
    已弃用。曾经用于一个目前不支持的跨网游新闻系统。


#define MUD_IS_LOCKED            0
    如果这是一个非零整数，只有允许组中的人员才被允许
登录。默认情况下，这些组是：SECURE、ASSIST、ELDER、TEST。


#define AUTO_WIZ                 0
    如果这是 1，新用户可以选择以玩家或创建者的
身份加入你的游戏。如果是 0 或 1 以外的任何值，
新用户将自动成为创建者，没有成为玩家的选项。


#define RESTRICTED_INTERMUD      0
    如果这是一个非零整数，用户必须由管理员添加到
INTERMUD 组中，才能使用跨网游频道。这可以通过
"groupmod" 命令方便地完成。


#define DISABLE_INTERMUD         0
    如果这是一个非零整数，所有跨网游功能将不起作用。
你的跨网游守护进程将拒绝加载，因此你将不会连接到
跨网游网络。注意在这里禁用跨网游会同时禁用 Intermud-3
和 IMC2。


#define DISABLE_IMC2             0
    IMC2 是一种跨网游协议，类似于 i3。它的数据包语法
不同，需要自己的处理程序和子系统。要使用它，
在这里保持启用并使用 imc2 命令。


#define MINUTES_REBOOT_WARNING   20
    不言自明。


#define TIME_TO_RESET            3600
    主守护进程中 eventReset() 调用之间的间隔。
20000 意味着每 5 小时，主守护进程将在自身调用
eventReset()。此函数将随机选择所有已加载对象的 25%，
并对它们调用 reset() 和 clean_up()。


#define RESET_ALL                0
    如果这是一个非零整数，MASTER_D 中的 eventReset()
将对 100% 的所有已加载对象执行 eventReset()，而不是 25%。
不推荐用于大型游戏，因为如果有很多已加载对象，
这可能会导致明显的延迟。


#define GMT_OFFSET               -32000
    一种调整时间设置的方法，如果你住在奇怪的地方。
建议：不要修改此项。


#define LOCAL_TIME               1
    如果这是一个非零整数，游戏将简单地相信系统认为的
时间。大多数操作系统可以被信任返回你想要的时间，
但如果你的服务器在另一个时区，或者你的操作系统
返回 GMT（即 UTC）偏移量而不是本地时间，
那么你可能需要将此项设置为 0 并调整 GMT_OFFSET
和 EXTRA_TIME_OFFSET。


#define EXTRA_TIME_OFFSET        0
    另一个调整本地时间的工具。以小时为单位设置，不是秒。
自行承担调整偏移设置的风险。你系统上的正确时间
是你自己的责任。


#define MAX_LOG_SIZE             150000
    日志轮转守护进程使用此参数作为备份日志的触发器。
当日志轮转检查发生时，大约每两小时（在事件守护进程
中设置，参见 "events" 命令），守护进程设置扫描的
任何大于此大小的日志文件将被重命名为唯一名称并归档。


#define MAX_NET_DEAD_TIME        1800
    允许玩家"已断开连接但未退出"的最大秒数。
连接断开的玩家通常会被移动到 ROOM_FREEZER，
在那里停留直到重新建立连接。如果在指定的秒数内
没有重新连接，他们的玩家对象将被强制退出。


#define MIN_HISTORY_SIZE         20
#define MAX_HISTORY_SIZE         30
    存储在玩家命令历史缓冲区中的行数。


#define LOGON_TIMEOUT            360
    如果用户在此秒数内未完成登录，他们的连接将被断开。


#define IDLE_TIMEOUT             3600
    如果玩家空闲此秒数，他们将被强制退出。


#define LINK_WAIT_PERIOD         1800
    不支持。这是一个多角色游戏控制系统的一部分，
在 Dead Souls 2 中可能有效也可能无效。


#define MAX_PASSWORD_TRIES       3
#define MIN_USER_NAME_LENGTH     2
#define MAX_USER_NAME_LENGTH     15
#define MAX_USER_CAP_NAME_LENGTH 20
    不言自明。"大写名称"表示玩家可能拥有的包含
特殊字符的特殊名称。此名称在角色创建时指定。


#define LOCKED_ACCESS_ALLOWED    ({ "SECURE", "ASSIST", "ELDER", "TEST" })
    当游戏被锁定时，这些用户组的成员被允许登录。


#define SUPPORTED_CLIENTS       ({ "amcp", "amcp/1.1" })
    不支持。这是一个远程管理协议，尚未在 Dead Souls 2
上运行。


#define HOUR_LENGTH              1200
#define DAY_LENGTH               20
    游戏时间定义。Dead Souls 的小时和天比"现实世界"的
小时和天短。这就是指定的地方。


#define MAX_NEWBIE_LEVEL         3
    低于此等级的玩家会得到特殊待遇。他们可以访问
特殊的新手区域，在黑暗中看得见，并理解所有语言。


#define AUTOSAVE_TIME            450
    用户自动保存之间的间隔。


#define MORTAL_POSITIONS         ({ "player", "high mortal", "ambassador" })
    不支持。角色系统的一部分，默认不适用于 Dead Souls 2。


#define DEFAULT_GRAVITY          1.0
    影响玩家可以携带的物品数量。


#define DEFAULT_ITEM_MESSAGE     "You do not see that here."
    不言自明。


#define RESET_INTERMUD           0
    如果这是一个非零整数，你的跨网游数据缓存将在重启时被清除。


#define ENGLISH_ONLY             0
    强制用户只使用英语。


#define HUMANS_ONLY              0
    阻止新用户选择人类以外的种族。


#define PINGING_MUDS             ({})
    定义 ping 守护进程用于定期 i3 连接状态检查的游戏。
如果为空，你只 ping 自己。建议保持为空。


#define PING_INTERVAL            290
    自动 i3 ping 之间的秒数。


#define ENABLE_ENCUMBRANCE       0
    如果这是一个非零整数，如果玩家携带物品，
他们将几乎不可能成功进行战斗。任何未穿戴或
未装备的物品都会对战斗造成重大障碍。


#define GLOBAL_MONITOR           0
    如果设置为 1，所有用户在连接时都会被自动监听，
他们的会话将记录在 /secure/adm 中。
    如果设置为 2，只有非管理员用户会被这样记录。


#define PLAYER_KILL              0
    必须将此项设置为非零整数，玩家杀戮区域才能
在游戏中工作。


#define NPC_CATCH_TELL_DEBUG     0
    如果是非零整数，任何 NPC 看到或听到的任何内容
都会被回显到 /domains/default/room/catchtell，以及
一些调试信息。建议：不要使用此项。它比你预期的
更嘈杂，会使繁忙的游戏产生延迟，实际上仅供
调试目的。


#define SAME_IP_MAX              4
    允许从一个 IP 建立的最大连接数。管理员以前登录过的
IP 被排除在此限制之外，要排除的 IP 可以在
/secure/cfg/ip_unrestrict.cfg 中指定（将 IP 添加在
各自的行上）。


#define COMPAT_MODE              1
    这使代码库能够使用定义将 TMI-2 和 Nightmare 3
等旧代码即时转换为可用的 DS2 代码。
参见 /secure/include/compat.h 和 /secure/sefun/legacy.c


#define RETAIN_ON_QUIT           1
    如果这是一个非零整数，当角色退出时，她的背包会被保存，
并在她重新登录时恢复给她。如果这是零，当角色退出时，
她的背包会被移动到她的环境中，重新登录时不会恢复给她。
    无论此项设置如何，唯一物品都会被移动到退出者的环境中。


#define MAX_COMMANDS_PER_SECOND  10
    为非管理员定义每秒允许的最大命令数。
这避免了玩家脚本的愚蠢行为，以及每秒"命令堆叠"
数百个命令时可能发生的一些错误，这些错误会破坏
每个人的游戏体验。


#define DEFAULT_PARSING          1
    如果这是一个非零整数，基于代码库的驱动程序解析器
扩展将尝试重新解析模糊命令，使 "drop book" 在你有
多个书时像 "drop a book" 一样解析。这会为每个需要
重新解析的模糊命令增加一些开销，所以如果你遇到
性能问题，请禁用它。


#define CALL_OUT_LOGGING         0
    这将记录 /daemon 和 /secure 文件系统之外的对象
使用 call_out() efun 的情况。建议：不要使用此项。
虽然它已被修复不会破坏事物，但这严格来说是一个
调试工具，用于排查错误，而不是"正常使用"的日志记录器。


#define EVENTS_LOGGING           1
    这里的非零整数将启用由事件守护进程发起的事件的日志记录。


#define DISABLE_REBOOTS          0
    这里的非零整数将导致你的游戏定期重启不发生。


#define BARE_EXITS               1
    这里的非零整数允许使用出口，使得输入 "house" 成为可能，
而不是 "go house" 或 "enter house"。这会为每个命令增加
一些开销，所以如果你遇到性能问题，请禁用它。


#define COMMAND_MATCHING         1
    这里的非零整数允许使用"最短唯一命令匹配"。
这意味着你可能能够 "gi book to knight" 而不是
"give book to knight"。这会为每个命令增加一些开销，
所以如果你遇到性能问题，请禁用它。


#define OBJECT_MATCHING          1
    这里的非零整数允许使用"最短唯一对象匹配"。
这意味着你可能能够 "give bo to kni" 而不是
"give book to knight"。这会为每个命令增加一些开销，
所以如果你遇到性能问题，请禁用它。


#define LIVEUPGRADE_SERVER       "Dead Souls"
    已弃用。指定用于 OOB 实时升级的游戏。OOB 实时升级
不再受支持，已被更快更安全的基于 Web 的实时升级系统取代。


#define CARRIAGE_RETURN          "\r"
    在某些情况下，这个讨厌的小字符必须从文件中删除，
而不删除指定要删除什么字符的东西。当文件需要清理
此字符时，此定义有助于避免问题。不要更改此项。


#define OLD_STYLE_PLURALS        1
    这里的非零整数允许你使用 "book 2" 或 "2.book"
而不是 "second book"。这会为每个命令增加一些开销，
所以如果你遇到性能问题，请禁用它。


#define NM_STYLE_EXITS           0
    这里的非零整数启用旧的"明显出口"行为，即以
[n, s, enter lodge] 的形式显示在长描述上方。
    否则，它们显示在长描述下方，形式为：
Obvious exits: north, south, enter lodge


#define WEB_SOURCE_IP               "204.209.44.12"
    指定实时升级期间使用的 IP。


#define WEB_SOURCE_NAME          "lpmuds.net"
    指定用于实时升级的网站。如果目标 Web 服务器
使用虚拟主机，这通常是必要的。


#define DESTRUCT_LOGGING         0
    如果设置为非零整数，这将启用对对象调用 destruct()
efun 的日志记录。强烈建议你不要启用此项，除非在
最严重的故障情况下。


#define ROUTER_NAME              "Alpha"
#define IRN_PASSWORD             "Alpha"
#define IRN_PASSWORD1            "Bravo"
#define IRN_PASSWORD2            "Charlie"
#define IRN_PASSWORD3            "Delta"
#define ROUTER_TESTING           1
    运行 Intermud-3 路由器作为 IRN（路由器间网络）中
节点的特定定义。除非你 100% 确定需要更改，
否则最好保留这些设置。


#define SEFUN_PLURALIZE          0
    指示你是否希望用同名的 sefun 覆盖驱动程序内置的
pluralize() efun。


#define LOG_REMOTE_CHANS         0
    控制远程（即跨网游）频道的日志记录。跨网游频道
可能非常嘈杂，如果你在商业主机上为磁盘空间付费，
记录它们可能导致空间不足。


#define LOG_LOCAL_CHANS          1
    控制游戏本地频道的日志记录。


#define IMC2_CLIENT_PW           "clientpass"
#define IMC2_SERVER_PW           "serverpass"
    IMC2 连接的密码。当你第一次安装游戏时，这些会自动
更改为随机字符串。如果你升级游戏或重新安装，请确保
保留旧的随机化密码的副本并将它们放入新的 config.h 中。


#define ENABLE_CGI               0
    确定内置 Web 服务器是否接受 CGI 请求。
CreWeb 运行需要此项。注意虽然 CreWeb 尝试避免
明显的安全问题，但维护游戏的安全是你的责任。


#define WWW_DIR_LIST             0
    确定是否允许在内置 Web 服务器上列出目录。
CreWeb 运行需要此项。参见上面关于安全性的说明。


#define ENABLE_CREWEB            0
    不言自明。


#define F_TERMINAL_COLOR         1
    对 FluffOS 奇怪问题的旧解决方法。可能不再需要，
但我懒得检查。


#define MAX_CALL_OUTS            750
    call_out() efun 是一种调度事件的方式。在 MudOS 中，
滥用此 efun 可能会导致问题，因为整体游戏性能可能
受到严重影响。在 FluffOS 上这不太令人担忧，但
失控的 callout 仍然威胁着你的游戏的可用性。
这里定义的数字是游戏停止接受来自非特权对象（如
/realms/ 中的对象）的新 callout 的阈值。
特权、代码库和域对象即使达到此阈值仍然可以发出
callout。


#define REQUIRE_QUESTING         1
    默认情况下，Dead Souls 游戏要求玩家不仅要有足够的
经验来升级，还要获得 PLAYERS_D 中指定的任务点数。
此定义控制该要求是否生效。


#define AUTO_ADVANCE             1
    旧版本的 Dead Souls 要求你访问冒险者大厅中的特定
NPC 并请求升级。如果此定义设置为非零整数，
当玩家达到 PLAYERS_D 中指定的下一级别的经验/任务点数
要求时，将自动升级，而无需去找 Dirk。


#define MAX_INVENTORY_SIZE       2048
    如果对象的物品列表达到此数量的物品，它将停止
接受额外的对象。这是为了防止因不小心的递归编码
而意外阻塞游戏。


#define MAX_ATTACKS_PER_HB       40
    如果你的创建者启用了神模式并将他们的属性和技能
提升到天文数字，然后试图互相 PK，此定义将防止你的
游戏因他们的愚蠢行为而锁定。


#define MEMUSE_SOFT_LIMIT        0
    如果游戏的内存使用达到此限制，它将在重置、垃圾回收
和无环境对象方面进入更积极的状态。非特权对象将被限制
克隆新对象，直到内存使用再次低于此限制。
重要说明：游戏的内存使用与操作系统分配的内存不同。
实际使用的内存量通常远低于 top 等程序中显示的
内存分配量。如果你要尝试这个，请从较低的数字开始，
如 10000000（1000 万字节，即 10 兆）。0 表示禁用此限制。


#define MEMUSE_HARD_LIMIT        0
    如果游戏的内存使用达到此限制，它将尝试尽可能多地
丢弃垃圾。如果在该尝试后内存使用仍然高于此限制，
游戏将在一分钟内安排重启。0 表示禁用此限制。


#define MAX_USERS                0
    LIB_CONNECT 自动确定你的游戏理论上能够处理多少玩家。
基于此，它建立了同时登录的最大玩家限制。
在某些情况下，这个估计过于慷慨，可能需要将最大玩家数
限制为你的服务器实际能处理的，而不是它理论上能做的。
MAX_USERS 定义覆盖 LIB_CONNECT 计算的最大值。
设置为 0 允许 LIB_CONNECT 自己计算最大值。
注意上面定义的 LOCKED_ACCESS_ALLOWED 组中的用户
不受玩家上限限制，可以登录。
    要了解当前限制：
eval return LIB_CONNECT->GetMaxPlayers()


#define MIN_PERF                 2000
    一些高级功能，如对象匹配和开销映射，要求你的
游戏服务器具有最低性能分数才能正常工作。这就是
设置该分数的地方。越接近 0，计算机越快。
2000 是一台相当慢的计算机。


#define MINIMAP                  1
    玩家是否可以将小地图作为房间描述数据的一部分显示。


#define WIZMAP                   1
    创建者是否可以将巫师地图作为房间描述数据的一部分显示。


#define GRID                     1
    游戏网格系统是否为你的游戏启用。这将被"最低性能"
分数覆盖。如果你的计算机太慢，无论此设置如何，
网格都会被禁用。


#define ENABLE_INSTANCES         0
    如果为 1，游戏将假定它是一个实例，并将对存档文件
应用特殊的命名约定。你的"开发实例"应将此项设置为 0。
你的"玩家实例"应将此项设置为 1。
如果你不知道这些东西是什么意思，请保持为 0。
更多信息：help instances


#endif /* s_config_h */
    结束配置。
