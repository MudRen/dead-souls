Dead Souls 中的调试

你制作了一些很酷的东西，但该死的，它不工作。
Dead Souls 代码库中有各种工具可以追踪问题的来源：


%^GREEN%^elog%^RESET%^

如果文件在你的主目录中，只需输入：elog

这将为你提供你的个人错误日志的最后几行。
警告行告诉你关于可以工作但应该以某种方式修复的代码。
不包含 "Warning" 一词的行是错误行：它们表示你的代码中
阻止其工作的一些内容。例如：

> %^GREEN%^update sample_room%^RESET%^

---
*Error in loading object '/realms/cratylus/area/room/sample_room'
Object: /secure/cmds/creators/update at line 148

'<function>' at /secure/save/creators/c/cratylus (<function>) at /:0
'cmdAll' at /secure/save/creators/c/cratylus (/lib/command.c) at line 84
'cmd' at /secure/cmds/creators/update at line 109
'eventUpdate' at /secure/cmds/creators/update at line 148
'CATCH' at /secure/cmds/creators/update at line 148
Trace written to /log/catch
/realms/cratylus/area/room/sample_room: Error in update
*Error in loading object '/realms/cratylus/area/room/sample_room'



	此输出让你知道出了问题，但没有具体说明是什么。
如果你查看你的错误日志，可能会看到更详细和有帮助的内容：


> %^GREEN%^elog%^RESET%^

/log/errors/cratylus:

/realms/cratylus/area/room/sample_room.c line 10: Undefined variable 'Sample'
/realms/cratylus/area/room/sample_room.c line 10: parse error



	现在你可以看到错误是我在第 10 行的语法。
然后我会使用 ed 检查代码，特别是第 9 到 11 行。
事实证明我忘记在房间名称周围放引号了，
所以解析器尝试将其用作变量，当然这是不可能的。

	如果相关文件在 /secure 中，你需要输入 elog secure，
如果在 /cmds 中，输入 elog cmds，以此类推。


dbxwhere 和 dbxframe

	两个有用的调试命令是 dbxframe 和 dbxwhere。
让我们看看我那个坏掉的 sample_room.c 文件。
我们从 dbxwhere 开始，它为我们列出了导致错误的
事件链。各个步骤称为帧。

> %^GREEN%^dbxwhere%^RESET%^
*Error in loading object '/realms/cratylus/area/room/sample_room'
Object: /secure/cmds/creators/update at line 148

#0: '<function>' at /secure/save/creators/c/cratylus (<function>) at /:0
#1: 'cmdAll' at /secure/save/creators/c/cratylus (/lib/command.c) at line 84
#2: 'cmd' at /secure/cmds/creators/update at line 109
#3: 'eventUpdate' at /secure/cmds/creators/update at line 148
#4: 'CATCH' at /secure/cmds/creators/update at line 148


输出与我们上面看到的更新错误类似，但在列举步骤时，
dbxwhere 让我们可以使用 dbxframe 获取给定错误帧的更详细信息：

> %^GREEN%^dbxframe 4%^RESET%^
------
    /secure/cmds/creators/update.c:148 - CATCH(0)
    ----------------------------------------------------------------
        if( args == base_name(this_object()) ) {
            this_player()->eventPrint("Cannot reload update after destruct.\n"
              "It will be reloaded at next reference.");
            return 0;
        }
    =>  tmp = catch(call_other(args, "???"));
        if( !tmp ) {
            if(identify(flags ^ U_AUTOMATED) ==
    "8")this_player()->eventPrint(args + ": Ok");
            return 1;
        } else this_player()->eventPrint(args + ": Error in update\n" + tmp);
        return 0;


    我们现在正在查看错误帧 4 的错误上下文。命令输出向我们显示
执行评估时出现错误的部分文件，甚至使用文本箭头指出有问题的行：=>

    在这个特定情况下，信息不是很有帮助。我们被告知错误发生在
使用 update 命令时，它在 update 执行操作的行失败了。
废话，我们早就知道了。elog 命令更有帮助。

    这种跟踪在你遇到运行时错误而不是更新文件时特别有用。
例如，如果我尝试进入那个房间而不是更新它，我会得到一个
很大的错误消息但不知道为什么。如果你遇到意外错误，
如果 elog 没有提供有用信息，dbxwhere 将帮助你定位它，
而 dbxframe 将帮助详细说明问题的来源。

%^GREEN%^tail%^RESET%^


    这是 unix tail 命令的版本。它显示文件的最后几行。
此命令对于检查关键日志文件很重要：

%^GREEN%^tail /log/catch%^RESET%^
%^GREEN%^tail /log/runtime%^RESET%^
%^GREEN%^tail /log/player_errors%^RESET%^


bk 和 restore

这些命令与其说是用于调试，不如说是用于安全编码。
在编辑文件之前，先备份它是一个非常好的主意。
bk 命令让你在编辑文件之前快速方便地备份它。
当我输入：

%^GREEN%^bk sample_room%^RESET%^

一个带有唯一标识号的文件在我的 bak/ 目录中创建。
如果我再次输入，sample_room.c 将再次被复制到 bak/，
名称中添加了一个新的唯一编号。

    这个数字基本上是自 1970 年 1 月 1 日以来经过的秒数。
添加这个数字，我们可以通过查看名称来跟踪哪个备份版本
是最新的。

    假设我编辑了一个名为 sample_npc.c 的文件。我使用 bk
备份它，做一些更改，然后再次使用 bk，再做一些更改，
但现在它无法更新了。我不想调试，我只需要这个文件再次
工作，所以我想从备份恢复。命令序列看起来像这样：

%^GREEN%^ed sample_npc.c%^RESET%^
%^GREEN%^bk sample_npc%^RESET%^
%^GREEN%^ed sample_npc.c%^RESET%^
%^GREEN%^update sample_npc.c%^RESET%^
<error occurs here>
%^GREEN%^restore sample_npc%^RESET%^

    使用标识号的原因是你也可以选择恢复文件的倒数第二个
备份版本，以及其他以前的版本。
    最后一个备份版本实际上是版本 0，所以不需要指定数字。
如果我想恢复我备份的前一个版本，我会输入类似这样的命令：

%^GREEN%^restore sample_npc 1%^RESET%^

    如果我想要再前一个版本，我会指定 2 而不是 1，以此类推。

    请注意，这是一个故意简单的系统。没有菜单、版本分支
或差异跟踪。原因是它不是一个版本控制系统。
它是一个备份系统。它是一个方便的撤消错误的工具，
而不是测试文件版本的开发工具。
