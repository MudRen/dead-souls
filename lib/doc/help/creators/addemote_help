[ 添加表情通用帮助 ]

注意：所有示例都是从"表情使用者"的角度来看的。
  基本表情:  $N smile $A.
    - $N 是表情使用者的名字。$N *必须*在 (emote) 和
      (environment) 字段中都使用，以保持功能。
      这意味着不要输入 'You smile $A.'。

    - $A 字段意味着用户可以从表情的副词库中指定
      任何副词。（这也适用于分配给该表情的任何
      特定副词。）如果用户没有指定副词，且没有设置
      默认副词，此字段将被忽略。

  针对目标的表情: $N smile $A at $T
    - $T 字段显示目标（玩家）的名字。如果没有找到
      目标，将使用"无目标"表情替代针对目标的表情。

  位置: $N pokes $A $T $X
    - $X 与 $A 的作用相同，只是它搜索分配给该表情的
      位置列表。如果用户没有指定位置，且存在默认设置，
      它将自动使用。否则，$X 将被忽略。
      注意所有位置前都添加了分配的介词（如果使用）。
      （例如：位置设置为 'nose, head, ear'，介词设置为
      'on the'。

  额外字段: $N scratch $*
    - 如果使用 $* 字段，所有剩余的参数将放在这里。

[ 有效字段列表 ]

    $N  - 表情使用者的名字
    $T  - 目标的名字
    $NP - 表情使用者的所有格 (his)
    $TP - 目标的所有格
    $NO - 表情使用者的宾格 (him)
    $TO - 目标的宾格
    $NR - 表情使用者的反身代词 (himself)
    $TR - 目标的反身代词
    $NA - 所有格形式 (name's)
    $TA - 目标的所有格名字
    $A  - 显示副词（如果用户未指定则忽略）
    $X  - 显示定义的位置
          （如果使用介词则添加介词前缀）
    $*  - 显示用户传递的任何额外参数
