影子是一种模拟向已存在对象添加函数的方式。

例如，"附魔"玩家的一种方式可能是让任何试图
对她造成伤害的对象都失败。这意味着你必须以
某种方式覆盖该玩家的 eventReceiveDamage
（还有其他方式，但让我们假装这是个好主意）。

影子可以做到这一点。你可以编写一个带有以下代码的对象：

varargs int eventReceiveDamage() {
return 0;
}

然后使用 shadow() efun 将其附加到玩家身上。
现在当某些东西试图调用玩家的 eventReceiveDamage
函数时，影子会拦截该调用，什么都不会发生。

很酷，对吧？

然而，影子存在问题，我们将在这里讨论：

1) 安全性
-----------
影子的主要问题是安全性。如果游戏中的任何人都可以
创建影子，那么你的游戏基本上没有任何安全性，
因为创建者可以影子化管理员（或其他特权对象），
从而提升自己的权限。

这将是很糟糕的。

在 Dead Souls 中的处理方式是，除非影子对象的代码
位于 /shadows 中，否则游戏不允许对象被影子化。
这意味着需要管理员将代码放在那里，这样至少有
一个*应该*知道自己在做什么的人负责将此代码投入
使用。

这种"只在 /shadows 目录中的东西"的行为在
valid_shadow() master apply 中定义。

2) 管理
-----------
影子的另一个头痛问题是管理它们。MudOS 的 shadow()
efun 使用起来足够简单，但如果一个对象有多个影子
而你只想移除一个，这就成问题了，因为 remove_shadow()
不区分影子...它会从对象上移除*所有*影子。
虽然这有时可能是可取的，但并不总是正确的事情。

从 Dead Souls 2.5a8 开始，包含了一个基本的多影子
管理系统。该系统依赖于游戏中的以下规则才能正常工作：

* 所有影子必须继承 LIB_SHADOW。
* 任何影子必须通过 eventShadow() lfun 附加到对象上。
* 任何影子必须通过 eventUnshadow() lfun 移除。

这是因为有形物品（午餐肉罐头、剑、玩家等）
现在都继承了 LIB_SHADOW_HOOK。这意味着任何要附加到
物品上的影子都会先向该物品表明自己的身份，
该物品将维护一个正在影子化它的对象列表。
这允许你查询该物品的影子，然后移除单个影子。
例如：

find_player("xyzzy")->GetShadows()
find_player("xyzzy")->eventUnshadow(find_object("/shadows/clan#9"))

这样只有那个特定的影子从 Xyzzy 身上被移除。

注意这个系统并不阻止人们独立使用 shadow() 和
remove_shadow()，所以为了让事情正常工作，你的游戏
需要将影子的移除和附加按照上述规则作为标准。

关于影子的一般信息，请参见：/doc/guide/chapter07
