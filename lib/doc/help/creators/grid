                      Dead Souls 网格系统

    Dead Souls 网格是一个动态生成的坐标系统，
它为每个房间分配 x,y,z 坐标（如果可能）。
当房间被加载时，它会向 ROOMS_D 查询自己的坐标。
如果它缺少坐标，它会检查相邻房间的坐标并尝试
计算自己的坐标，然后报告给 ROOMS_D。

    带坐标房间的想法是允许跨房间工作的游戏机制，
如定向传送、跨房间战斗等。不是每个游戏都需要它，
如果你不想使用，可以在 config.h 中禁用它。

    如果你创建了一个新域但它似乎没有被包含在网格中，
请确保你的域有一个名为 room/start.c 的房间，
并将该房间的坐标设置为对游戏其他坐标有意义的值，
或者通过远离其他所有东西来确保不会与现有坐标冲突。
在 start.c 房间的 create() 函数中，确保你有一个
类似这样的指令：

SetCoordinates("9000,7000,0");

    当然，要使用适合你的游戏的 x,y,z 坐标。

    完成此操作后，擦除现有的网格缓存并开始使用映射
无人机重建它。执行此操作的命令是：

gridconfig reset

    网格重建需要一些时间，具体取决于你的游戏大小。
如果启用了该功能，你应该能够在巫师地图中看到重建的进度。

另见: gridconfig, wizmap, minimap
