#include <lib.h>
#include <dirs.h>
#include <rooms.h>
#include <virtual.h>
#include <position.h>
#define __DIR__ "/domains/CHANGEME/virtual/"

inherit LIB_VIRT_LAND;

varargs static void create(int x, int y) {
    SetNoReplace(1);
    virt_land::create();
    SetClimate("temperate");
    SetAmbientLight(30);
    SetLong("一片空旷的虚无。");
    SetShort("一个空旷的地方");
    AddExit("down", ROOM_START);
}
